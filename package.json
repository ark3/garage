{
  "name": "garage",
  "private": true,
  "workspaces": [
    "bay",
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --cwd bay dev",
    "dev:test": "bun run --cwd bay dev:test"
  },
  "dependencies": {
    "@garage/sync": "workspace:*",
    "yjs": "13.6.32"
  }
}
