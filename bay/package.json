{
  "name": "bay",
  "private": true,
  "scripts": {
    "dev": "XDG_CONFIG_HOME=$PWD/.wrangler/xdg WRANGLER_LOG_PATH=$PWD/.wrangler/logs wrangler dev",
    "build:scratch": "bun build scratch/main.ts --outdir public",
    "build:notes": "mkdir -p public/notes && cp ../apps/notes/index.html public/notes/ && bun build ../apps/notes/src/main.ts --outdir public/notes",
    "build:backup": "mkdir -p public/backup && cp ../apps/backup/index.html public/backup/ && bun build ../apps/backup/src/main.ts --outdir public/backup"
  },
  "dependencies": {
    "@garage/sync": "workspace:*",
    "lib0": "0.2.117",
    "y-protocols": "1.0.7",
    "yjs": "13.6.32"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260828.1",
    "wrangler": "4.127.1"
  }
}
